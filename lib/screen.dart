import 'package:flutter/material.dart';
import 'package:demineur/screens/home_screen.dart';

class Demineur extends StatelessWidget {
    const Demineur({super.key});

    @override
    Widget build(context) {
      return const Scaffold(
        body: Text('bjr')
      );
    }
}

